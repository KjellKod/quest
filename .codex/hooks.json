{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "apply_patch",
        "hooks": [
          {
            "type": "command",
            "command": "echo '📍 Branch:' $(git branch --show-current 2>/dev/null || echo 'no git') '| Dir:' $(pwd)"
          }
        ]
      }
    ]
  }
}
