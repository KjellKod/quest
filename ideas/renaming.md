Suggestion for quest: when installed, it adds several scripts files into the repo root's script/ folder, which is a fairly common tracked path across repositories and requires users to  add all of the added paths to .gitignore. I'd be nice if all the quest scripts would instead be instealled in scripts/quest/<here> or that they are all consistently prefixed with `quest_` like many of them already are
